{
  "name": "dynamo-iterator",
  "version": "1.1.3",
  "description": "Iterator pattern applied to AWS Dynamo searches",
  "main": "index.js",
  "repository": "git@github.com:AitorGuerrero/dynamo-iterator.git",
  "author": "Aitor Guerrero <aitgud.dev@gmail.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "test": "mocha",
    "test:coverage": "nyc mocha",
    "build": "tsc",
    "check:style": "tslint -p .",
    "preversion": "yarn build && yarn test:coverage"
  },
  "devDependencies": {
    "@types/chai": "^4.2.5",
    "@types/mocha": "^7.0.2",
    "@types/node": "^13.13.5",
    "aws-sdk": "^2.580.0",
    "chai": "^4.2.0",
    "husky": "^4.2.5",
    "mocha": "^7.1.2",
    "nyc": "^15.0.1",
    "source-map-support": "^0.5.19",
    "ts-node": "^8.10.1",
    "tslint": "^5.20.1",
    "typescript": "^3.7.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn install && yarn check:style && yarn build && yarn test:coverage"
    }
  }
}
