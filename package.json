{
  "name": "dynamo-iterator",
  "version": "1.1.2",
  "description": "Iterator pattern applied to AWS Dynamo searches",
  "main": "index.js",
  "repository": "git@github.com:AitorGuerrero/dynamo-iterator.git",
  "author": "Aitor Guerrero <aitgud.dev@gmail.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "test": "mocha",
    "build": "tsc",
    "check:style": "tslint -p .",
    "check:builds": "tsc --noEmit",
    "preversion": "yarn build"
  },
  "devDependencies": {
    "@types/chai": "^4.2.5",
    "@types/mocha": "^7.0.2",
    "@types/node": "^13.13.5",
    "aws-sdk": "^2.580.0",
    "chai": "^4.2.0",
    "husky": "^4.2.5",
    "mocha": "^7.1.2",
    "nyc": "^15.0.1",
    "tslint": "^5.20.1",
    "typescript": "^3.7.2"
  },
  "husky": {
    "hooks": {
      "pre-commit": "yarn && yarn run check:style && yarn run check:builds"
    }
  }
}
